- avatar_size ||= 32
p.avatar = link_to_user_by_avatar comment.user, size: avatar_size
- if user_signed_in? && current_user.id == comment.user_id
  p.body = uncomment_to comment
- else
  p.body = comment_to comment
p.time = "#{time_ago_in_words comment.created_at}前"
